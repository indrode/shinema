= form_for @movie_list do |f|
  - if @movie_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@movie_list.errors.count, "error")} prohibited this movie_list from being saved:"
      %ul
        - @movie_list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
